package com.smoothstack.usermicroservice.service;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class SendGridEmailServiceTest {
    // May be out of scope without a proper testing integration
    // that can receive mock emails and report on their status.
}
